export async function solveCaptchaIfPresent(page) {
  // Placeholder: aquí irá tu integración con CapMonster o 2Captcha
  console.log('🔐 Verificando captcha (mock)...');
  await page.waitForTimeout(1000); // Simula espera de resolución
}
